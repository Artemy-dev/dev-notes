![kafka](https://github.com/Artemy-dev/dev-notes/blob/main/backend/kafka.jpg)

**Kafka** - это брокер сообщений, ориентированный на события. 
`Продюсеры (Producers)` публикуют `события (Events)` в топики, `Kafka (Broker)` их хранит, а `консюмеры (Consumers)` читают эти события независимо друг от друга. 
В отличие от RabbitMQ, Kafka чаще используют для потоков событий, аналитики и высоконагруженных систем.
